# Used by browser requests in local development. Leave empty in production so /api is same-origin.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api

# Used by Vinext server components on the VPS.
AFQ_API_INTERNAL_URL=http://127.0.0.1:8001/api

HOST=127.0.0.1
PORT=3000
